#include<iostream>
using namespace std;
int main()
{
    int arr[] = { 9, 4, 9, 6, 7, 4 };
    int i,j;
    int n = sizeof(arr) / sizeof(arr[0]);
    for(i=0;i<n;i++){
    for(j=0;j<n;j++){
    
        if(i!=j&&arr[i]==arr[j])
        break;}
        if (j == n)
            cout<<arr[i]<<endl;
    }
    return 0;
}
